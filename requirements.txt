flask==3.1.3
flask-sqlalchemy==3.1.1
flask-login==0.6.3
flask-wtf==1.2.2
flask-bcrypt==1.0.1
Flask-Limiter==3.5.0
flask-openapi3>=3.0.0
pydantic>=2.0.0
# authlib 1.7.x ships an OAuth client change that mis-handles the
# client_id claim placement for some OpenID Connect providers, causing
# `unsupported_header: Unsupported {'client_id'} in header` on the SSO
# callback (issue #298; upstream authlib/authlib#902). Pinned to the
# last known-good 1.6.x line until the upstream fix is released.
authlib>=1.3.0,<1.7.0
itsdangerous>=2.1.0
email-validator==2.2.0
openai>=2.2.0
httpx>=0.27.0,<1.0.0
pywebpush==1.14.0
werkzeug==3.1.6
gunicorn==22.0.0
python-dotenv==1.2.2
markdown==3.8.1
pytz==2024.1
Babel==2.12.1
bleach==6.4.0
python-docx==1.1.0
numpy==1.24.3
scikit-learn==1.5.0
# KCS/GH SCA: was scipy<1.15 (resolver guessed 1.9.3 -> CVE-2023-25399); pin exact patched 1.14.1.
scipy==1.14.1
psycopg2-binary>=2.9.0
boto3>=1.34.0
# KCS/GH SCA: openai (transitive) pulls httpx2/httpcore2; floating range made
# osv.dev resolve pre-fix 2.9.1. The image installs 2.12.0 — pin it explicitly
# (covers CVE-2026-84378/79/80 @2.10-2.11 and CVE-2026-84382 @2.12.0).
httpx2==2.12.0
httpcore2==2.12.0
